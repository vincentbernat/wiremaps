# git-ls-files --others --exclude-from=.git/info/exclude
# Lines that start with '#' are comments.
# For a project mostly in C, the following would be a good set of
# exclude patterns (uncomment them if you want to use them):
# *.[oa]
# *~
*~
*.pyc
*.so
/build/
dropin.cache
/twistd.log
/wiremaps/web/static/stamp-ports

/debian/wiremaps/
/debian/wiremaps.substvars
/debian/wiremaps.*.debhelper
/debian/wiremaps.debhelper.log
/debian/files
/debian/python-module-stampdir/wiremaps
